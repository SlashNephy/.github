name: 'Check env/amq'

on:
  push:
    branches:
      - 'master'
    paths:
      - 'env/amq/**'
      - '.github/workflows/_check-amq.yml'

  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - 'env/amq/**'
      - '.github/workflows/_check-amq.yml'

  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/yarn-lint.yml
    with:
      pre-script: 'workspace @slashnephy/types-amq build'
      script: 'workspace @slashnephy/types-amq lint'
    permissions:
      contents: 'read'
      pull-requests: 'write'

  format:
    if: github.event_name != 'pull_request'
    uses: ./.github/workflows/yarn-format.yml
    with:
      pre-script: 'workspace @slashnephy/types-amq build'
      script: 'workspace @slashnephy/types-amq format'
    permissions:
      contents: 'write'

  publish:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: lint
    uses: ./.github/workflows/yarn-publish.yml
    with:
      pre-script: 'workspace @slashnephy/types-amq bump-version'
      script: 'workspace @slashnephy/types-amq publish'
      commit-message: '🔨 Bump version'
    permissions:
      contents: 'write'
    secrets:
      NODE_AUTH_TOKEN: '${{ secrets.NPM_ACCESS_TOKEN }}'
