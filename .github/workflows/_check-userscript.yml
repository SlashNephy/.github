name: 'Check env/userscript'

on:
  push:
    branches:
      - 'master'
    paths:
      - 'env/userscript/**'
      - '.github/workflows/_check-userscript.yml'

  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - 'env/userscript/**'
      - '.github/workflows/_check-userscript.yml'

  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/yarn-run.yml
    with:
      working-directory: 'env/userscript'
      script: 'build'
    permissions:
      contents: 'read'

  lint:
    uses: ./.github/workflows/yarn-lint.yml
    with:
      working-directory: 'env/userscript'
    permissions:
      contents: 'read'
      pull-requests: 'write'

  format-and-build-ci:
    if: github.event_name != 'pull_request'
    uses: ./.github/workflows/yarn-format.yml
    with:
      working-directory: 'env/userscript'
      script: 'ci'
      commit-message: '⚙️ Build (env/userscript)'
    env:
      NODE_OPTIONS: --max-old-space-size=4096
    permissions:
      contents: 'write'
