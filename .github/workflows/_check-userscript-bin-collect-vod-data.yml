name: 'Check env/userscript/bin/collect-vod-data'

on:
  push:
    branches:
      - 'master'
    paths:
      - 'env/userscript/bin/collect-vod-data/**'
      - '.github/workflows/_check-userscript-bin-collect-vod-data.yml'
      - '!env/userscript/bin/collect-vod-data/dist/**'

  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - 'env/userscript/bin/collect-vod-data/**'
      - '.github/workflows/_check-userscript-bin-collect-vod-data.yml'
      - '!env/userscript/bin/collect-vod-data/dist/**'

  schedule:
    # JST 4:00 AM
    - cron: '0 19 * * *'

  workflow_dispatch:

jobs:
  update:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: '${{ secrets.BOT_GITHUB_TOKEN }}'

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version-file: 'env/userscript/bin/collect-vod-data/go.mod'
          cache: true
          cache-dependency-path: 'env/userscript/bin/collect-vod-data/go.sum'

      - name: Generate
        run: go run ./
        working-directory: 'env/userscript/bin/collect-vod-data'

      - name: Pull
        run: git pull

      - name: Push
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: '⚙️ Build (env/userscript/bin/collect-vod-data)'
          commit_author: 'StarryBlueSky-bot <97773209+StarryBlueSky-bot@users.noreply.github.com>'
