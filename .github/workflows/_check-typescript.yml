name: 'Check env/typescript'

on:
  push:
    branches:
      - 'master'
    paths:
      - 'env/typescript/**'
      - '.github/workflows/_check-typescript.yml'

  pull_request:
    types:
      - opened
      - synchronize

  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/yarn-run.yml
    with:
      working-directory: 'env/typescript'
      script: 'build'
    permissions:
      contents: 'read'

  lint:
    uses: ./.github/workflows/yarn-lint.yml
    with:
      working-directory: 'env/typescript'
    permissions:
      contents: 'read'
      pull-requests: 'write'

  format:
    if: github.event_name == 'push'
    uses: ./.github/workflows/yarn-format.yml
    with:
      working-directory: 'env/typescript'
    permissions:
      contents: 'write'
