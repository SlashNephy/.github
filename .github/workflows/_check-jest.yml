---
name: 'Check env/jest'

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - 'master'
    paths:
      - 'env/jest/**'
      - '.github/workflows/_check-jest.yml'

  pull_request:
    types:
      - opened
      - synchronize

  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/yarn-run.yml
    with:
      working-directory: 'env/jest'
      script: 'build'
    permissions:
      contents: 'read'

  lint:
    uses: ./.github/workflows/yarn-lint.yml
    with:
      working-directory: 'env/jest'
    permissions:
      contents: 'read'
      pull-requests: 'write'

  format:
    if: github.event_name == 'push'
    uses: ./.github/workflows/yarn-format.yml
    with:
      working-directory: 'env/jest'
    permissions:
      contents: 'write'
