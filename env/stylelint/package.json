{
  "name": "@slashnephy/stylelint-config",
  "version": "0.1.1",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/SlashNephy/.github",
    "directory": "env/stylelint"
  },
  "license": "MIT",
  "files": [
    "package.json",
    "dist/*.js",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/",
    "format": "concurrently -n format: 'yarn:format:*'",
    "format:eslint": "yarn lint:eslint --fix",
    "format:prettier": "yarn lint:prettier --write",
    "lint": "concurrently -n lint: 'yarn:lint:*'",
    "lint:eslint": "eslint .",
    "lint:prettier": "prettier --check .",
    "publish": "yarn clean && yarn build && yarn npm publish"
  },
  "dependencies": {
    "stylelint-config-property-sort-order-smacss": "9.1.0",
    "stylelint-config-recommended": "13.0.0"
  },
  "peerDependencies": {
    "stylelint": "^15"
  },
  "devDependencies": {
    "@slashnephy/eslint-config": "2.0.46",
    "@slashnephy/prettier-config": "1.0.31",
    "@types/node": "20.4.5",
    "concurrently": "8.2.0",
    "eslint": "8.45.0",
    "prettier": "3.0.0",
    "stylelint": "15.10.2",
    "typescript": "5.1.6"
  },
  "packageManager": "yarn@3.6.1",
  "eslintConfig": {
    "extends": [
      "@slashnephy/eslint-config"
    ]
  }
}
