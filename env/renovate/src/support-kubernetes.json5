{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  description: 'Support Kubernetes',
  kubernetes: {
    extends: ['default:automergeDigest'],
    fileMatch: ['\\.yaml$'],
    pinDigests: true,
    pinDigest: {
      extends: ['default:automergeBranchPush'],
    },
  },
  kustomize: {
    pinDigests: true,
    pinDigest: {
      extends: ['default:automergeBranchPush'],
    },
  },
}
