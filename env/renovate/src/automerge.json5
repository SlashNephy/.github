{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  description: '特定のパッケージを Automerge する',
  packageRules: [
    {
      matchPackageNames: ['@kawaiioverflow/arm', 'renovate', '@slashnephy/eslint-config', '@slashnephy/prettier-config'],
      automerge: true,
      ignoreTests: true,
    }
  ],
}
